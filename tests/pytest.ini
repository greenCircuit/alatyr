[pytest]
# Default run excludes perf — functional suite stays fast and perf timings
# don't gate it. Run the perf sweep explicitly: pytest -m perf
addopts = -m "not perf"
markers =
    perf: initial-load latency sweep over cluster size (opt-in, not run by default)
