# Real observability namespace dump — showcases what the visualizer surfaces in production.
# 9 running workloads, only some covered by NetworkPolicies — unprotected ones render as internet-full.
---
apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    kubernetes.io/metadata.name: observability
    app.kubernetes.io/component: observability
