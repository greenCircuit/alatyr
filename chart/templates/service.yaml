apiVersion: v1
kind: Service
metadata:
  name: {{ include "netpol-viz.fullname" . }}
  labels:
    {{- include "netpol-viz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "netpol-viz.selectorLabels" . | nindent 4 }}
