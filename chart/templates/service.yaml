apiVersion: v1
kind: Service
metadata:
  name: {{ include "alatyr.fullname" . }}
  labels:
    {{- include "alatyr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.serviceMonitor.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "alatyr.selectorLabels" . | nindent 4 }}
